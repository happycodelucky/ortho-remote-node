/**
 * Default discovery timeout 60 seconds
 * @internal
 */
export const DEVICE_DISCOVERY_TIMEOUT_MS = 60 * 1000

/**
 * Default connection timeout 10 seconds
 * @internal
 */
export const DEVICE_CONNECT_TIMEOUT_MS = 10 * 1000